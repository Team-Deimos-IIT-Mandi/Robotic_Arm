name: "🤖 Debug ROS Container Build"

on:
  push:
    branches: [ "main", "develop", "feature/anish" ] # Trigger on your branch

jobs:
  debug-build:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout repository code"
        uses: actions/checkout@v4

      - name: "Attempt to build the Docker image"
        run: docker build -t debug-image .